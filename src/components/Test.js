import React from 'react'
import NavBar from './NavBar'

const Test = () => {
  return (
    <div>
        <NavBar />
        ここで4択クイズを行う
        {/* 四つ時計を設置し、ラジオボタンで回答 */}
        {/* 正解した場合と外れた場合で分岐 */}
        {/* LectureへのリンクとExerciseへのリンク */}
    </div>
  )
}

export default Test